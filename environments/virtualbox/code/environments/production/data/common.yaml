baseconfig::params::pkg_ntp: true

baseconfig::params::linux_users:
  - user:
      name: matheus
      ensure: present
  - user:
      name: deploy
      ensure: present
  - user:
      name: teste
      ensure: absent
  - user:
      name: fulano
      ensure: absent
